{
  "name": "sales-app",
  "version": "1.0.0",
  "description": "sales REST API",
  "main": "server.js",
  "scripts": {
    "docker": "docker compose up -d",
    "dir": "(mkdir public && mkdir public/files && mkdir public/files/photos && mkdir public/files/temp)",
    "local": "(bash scripts/env.sh && nodemon src/server.js)"
  },
  "author": "adrian",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "sharp": "^0.33.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
